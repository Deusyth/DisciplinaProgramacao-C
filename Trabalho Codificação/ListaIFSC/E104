#include <stdio.h>

int main() {
    int a, b, resultado = 0;

    printf("Digite dois números inteiros: ");
    scanf("%d %d", &a, &b);

    for (int i = 0; i < b; i++) {
        resultado += a;
    }

    printf("Resultado: %d\n", resultado);
    return 0;
}

#include <stdio.h>

int main() {
    int a = 0, b = 1, c;

    printf("Série de Fibonacci:\n");
    printf("%d %d ", a, b);

    for (int i = 2; i < 20; i++) {
        c = a + b;
        printf("%d ", c);
        a = b;
        b = c;
    }

    return 0;
}
